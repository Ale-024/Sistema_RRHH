import { useCallback, useEffect, useState } from 'react';
import { Plus, Settings, XCircle } from 'lucide-react';
import api from '../services/api';
import { tienePermiso } from '../shared/roles';
import { useAuthStore } from '../store/useAuthStore';

export default function AdminParameters() {
  const { user } = useAuthStore(); const [items, setItems] = useState([]); const [modal, setModal] = useState(false); const [mensaje, setMensaje] = useState(''); const [form, setForm] = useState({ clave: '', valor: '', unidad: 'MONTO_CENT', vigenciaDesde: '', descripcion: '', baseLegal: '' });
  const cargar = useCallback(async () => { try { setItems((await api.get('/admin/parametros-legales')).data); } catch { setMensaje('No fue posible cargar los parámetros.'); } }, []);
  useEffect(() => {
    // react(set-state-in-effect) es intencional: sincroniza datos remotos al montar.
    // eslint-disable-next-line react-hooks/set-state-in-effect
    cargar();
  }, [cargar]);
  const crear = async (event) => { event.preventDefault(); try { await api.post('/admin/parametros-legales', form); setModal(false); setMensaje('Nueva vigencia guardada.'); setForm({ clave: '', valor: '', unidad: 'MONTO_CENT', vigenciaDesde: '', descripcion: '', baseLegal: '' }); await cargar(); } catch (error) { setMensaje(error.response?.data?.detail || 'No fue posible guardar el parámetro.'); } };
  return <div className="animate-in fade-in duration-500 space-y-6"><div className="flex items-end justify-between"><div><h1 className="text-2xl font-bold text-slate-900">Parámetros legales</h1><p className="mt-1 text-slate-500">Las nuevas vigencias afectan únicamente cálculos futuros.</p></div>{tienePermiso(user, 'parametros:administrar') && <button onClick={() => setModal(true)} className="flex items-center rounded-lg bg-brand-blue px-4 py-2 font-medium text-white"><Plus className="mr-1 h-5 w-5" /> Nueva vigencia</button>}</div>{mensaje && <div className="rounded-lg bg-blue-50 p-4 text-sm text-blue-700">{mensaje}</div>}<div className="overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm"><table className="w-full text-left text-sm text-slate-600"><thead className="border-b border-slate-200 bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="px-6 py-4">Clave</th><th className="px-6 py-4">Valor</th><th className="px-6 py-4">Unidad</th><th className="px-6 py-4">Vigencia</th></tr></thead><tbody>{items.map((item) => <tr key={item.id} className="border-b border-slate-100"><td className="px-6 py-4 font-medium text-slate-900">{item.clave}</td><td className="px-6 py-4">{item.valor}</td><td className="px-6 py-4">{item.unidad || '—'}</td><td className="px-6 py-4 text-xs">{new Date(item.vigenciaDesde).toLocaleDateString()} — {item.vigenciaHasta ? new Date(item.vigenciaHasta).toLocaleDateString() : 'abierta'}</td></tr>)}{!items.length && <tr><td colSpan="4" className="p-12 text-center text-slate-400"><Settings className="mx-auto mb-2 h-10 w-10 text-slate-300" />No hay parámetros.</td></tr>}</tbody></table></div>{modal && <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-900/50 p-4"><div className="w-full max-w-lg rounded-xl bg-white shadow-xl"><div className="flex items-center justify-between border-b border-slate-100 px-6 py-4"><h3 className="font-bold text-slate-900">Nueva vigencia</h3><button onClick={() => setModal(false)} aria-label="Cerrar"><XCircle className="h-6 w-6 text-slate-400" /></button></div><form onSubmit={crear} className="grid gap-4 p-6"><input required placeholder="Clave" value={form.clave} onChange={(event) => setForm({ ...form, clave: event.target.value })} className="rounded-lg border border-slate-300 p-2.5" /><div className="grid grid-cols-2 gap-4"><input required placeholder="Valor" value={form.valor} onChange={(event) => setForm({ ...form, valor: event.target.value })} className="rounded-lg border border-slate-300 p-2.5" /><input required placeholder="Unidad" value={form.unidad} onChange={(event) => setForm({ ...form, unidad: event.target.value })} className="rounded-lg border border-slate-300 p-2.5" /></div><label className="text-sm text-slate-600">Vigencia desde<input required type="date" value={form.vigenciaDesde} onChange={(event) => setForm({ ...form, vigenciaDesde: event.target.value })} className="mt-1 w-full rounded-lg border border-slate-300 p-2.5" /></label><input placeholder="Descripción" value={form.descripcion} onChange={(event) => setForm({ ...form, descripcion: event.target.value })} className="rounded-lg border border-slate-300 p-2.5" /><input placeholder="Base legal" value={form.baseLegal} onChange={(event) => setForm({ ...form, baseLegal: event.target.value })} className="rounded-lg border border-slate-300 p-2.5" /><div className="flex justify-end gap-3"><button type="button" onClick={() => setModal(false)} className="rounded-lg px-4 py-2 text-slate-600">Cancelar</button><button type="submit" className="rounded-lg bg-brand-blue px-4 py-2 font-medium text-white">Guardar</button></div></form></div></div>}</div>;
}
