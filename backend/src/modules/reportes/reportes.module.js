const express = require('express');

/**
 * Andamiaje del modulo reportes. Proyecciones y exportacion: Fase 7.
 */
function rutasReportes(_ctx) {
  return express.Router();
}

module.exports = { rutasReportes };
